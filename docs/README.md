# machine docs

Install and hardware. Datasheets: `/drone/docs`, `/sentry/docs`.

| File | What |
|---|---|
| `../README.md` | map, rebuild |
| `../install.sh` | wipe nvme0n1 and install |
| `../hardware-configuration.nix` | disk, initrd, microcode |
| `install.md` | live-ISO install notes |
